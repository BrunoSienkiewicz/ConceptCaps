# Project paths
# Override these in a local .env file (not tracked by git)
PROJECT_ROOT=/home/bruno/projects/music-gen-interpretability
DATA_DIR=${PROJECT_ROOT}/data
MODELS_DIR=${PROJECT_ROOT}/models
OUTPUTS_DIR=${PROJECT_ROOT}/outputs
LOGS_DIR=${PROJECT_ROOT}/logs

# Weights & Biases (optional)
# Sign up at https://wandb.ai to get your entity name
WANDB_PROJECT=music-gen-interpretability
WANDB_ENTITY=

# HuggingFace (optional)
# Set if you need to access gated models
HF_TOKEN=

# SLURM (if using HPC cluster)
SLURM_ACCOUNT=
SLURM_PARTITION=
