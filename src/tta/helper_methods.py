import torch
from torch.utils.data import Dataset


def sample_dataset(dataset: Dataset) -> list[str]:
    pass

def preprocess_dataset(inputs: list[str]) -> torch.Tensor:
    pass

def generate_audio(model, inputs):
    pass